# Database configuration (required)
DATABASE_URL=postgres://postgres:pw@localhost:5432/llmgateway

# Authentication (required)
AUTH_SECRET=your-secret-key

# Service URLs
UI_URL=localhost:3002
API_URL=localhost:4002
AGENTIC_API_URL=localhost:4000
AGENTIC_UI_URL=localhost:3000
ORIGIN_URL=localhost:3000,localhost:3002
DOCS_URL=localhost:3005

# Optional: PostHog analytics
POSTHOG_KEY=phc_123
POSTHOG_HOST=https://app.posthog.com

# Optional: Redis for caching (if implementing agent memory)
REDIS_URL=redis://localhost:6379